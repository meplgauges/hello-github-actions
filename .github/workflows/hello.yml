name: Hello World Workflow

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print Hello World
        run: echo "👋 Hello, GitHub Actions on Ubuntu!"
        
      - name: Print Welcome
      - run: echo "Welcome"
